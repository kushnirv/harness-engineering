# Architecture — <PROJECT_NAME>

> Читать по требованию (Read): `.claude/docs/ARCHITECTURE.md`, когда агент работает с архитектурой.
> НЕ `@import` — `@`-ссылка грузит файл в контекст на СТАРТЕ, не лениво.

## Стек

- **Язык:** <LANG_AND_VERSION>
- **Фреймворк:** <FRAMEWORK_AND_VERSION>
- **Сборка:** <BUILD_TOOL>
- **Тесты:** <TEST_FRAMEWORK>
- **Доп. инструменты:** <OTHER_TOOLS>

## Структура

```
<WATCH_DIR>/
  <MODULE_DIR>/     ← основная единица работы (компонент / сервис / пакет / …)
  <SHARED_DIR>/     ← общий код (типы, утилиты, константы)
  <ENTRY_POINT>     ← точка входа
```

<!-- Замени на реальную структуру своего стека.
     core-ok: три стека примером в заготовке, которую потребитель переписывает под себя.
     Vue: components/ + composables/. Go: cmd/ + internal/ + pkg/.
     PHP: src/ + tests/. И т.д. -->

## Паттерны

<!-- Ключевые инварианты ИМЕННО этого проекта, которые агент не выведет из кода.
     Языковые правила (синтаксис, идиомы) — в .claude/rules/lang/<lang>.md. -->

- **Единица работы:** <UNIT_PATTERN>
- **Контракт/типы:** <TYPING_RULE>
- **Маркер QA:** <QA_MARKER> (как тесты находят сущность)

## Модель данных

<!-- Сущности и связи. При разборе чужого кода сюда идёт находка «схема данных»:
     модели, миграции, где источник правды. Инвариант формулировать через
     отсутствие — «заказ не хранит цену товара, берёт из позиции». -->

- **Сущности:** <ENTITIES>
- **Источник правды:** <DATA_SOURCE>
- **Чего в модели НЕТ:** <DATA_INVARIANT>

## Бизнес-логика

<!-- Правила, которые не выводятся из типов: переходы статусов, права, расчёты.
     Основной сценарий агент прочитает в коде — сюда развилки и краевые случаи. -->

- **Основной сценарий:** <MAIN_FLOW>
- **Переходы состояний:** <STATE_TRANSITIONS>
- **Права:** <ACCESS_RULES>

## Запрещённые зоны (readonly)

```
<READONLY_ZONES>
```

Запись в эти директории блокируется guard-хуком (exit 2).

## Эталон

`<REFERENCE_PATH>` — рабочий пример, копировать структуру.

## Связь с системой

- Тесты: `<TEST_CMD>`
- Скаффолдинг: языковой пакет, если его накладывали (каталога `lang-packs/` в проекте нет —
  он живёт в репозитории шаблона, оттуда копируют скилы в `.claude/skills/`)
- Память: `.claude/docs/dev-guide.md` (если есть в твоём lang-pack)
